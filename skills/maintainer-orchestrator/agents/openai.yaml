interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate repo queues, workers, and releases"
  default_prompt: "Use $maintainer-orchestrator to observe current worker state, require real live proof before landing, coordinate only confirmed blockers or idle lanes, and release only after the effective queue is empty and CI is green."
