interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate repo queues, workers, and releases"
  default_prompt: "Use $maintainer-orchestrator to coordinate repository queues, delegate autonomous work, surface owner-only blockers, and release only after the effective queue is empty and CI is green."
